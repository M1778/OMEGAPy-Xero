# SoreUI - Desktop GUI Dependencies
# Requires Python 3.10+ and a display server (X11/Wayland/macOS)

# GUI Framework
PyQt6>=6.5,<7.0
setuptools>=70.0,<81.0

# Local library (install manually - not on PyPI)
# omglib==0.1.45

# Audio
pyaudio>=0.2.14
sounddevice>=0.4,<1.0
numpy>=1.24

# Speech
pyttsx3>=2.90,<3.0
gTTS>=2.3,<3.0
SpeechRecognition>=3.10,<4.0
webrtcvad>=2.0,<3.0

# NOTE: openwakeword is optional and disabled if unavailable.
# It has no Linux Python 3.14 wheels (needs tflite-runtime).
# Install on Windows/Python<3.13: pip install openwakeword>=0.6,<1.0
# omglib is required and must be installed separately (not on PyPI).

# System
psutil>=5.9,<6.0

# Markdown
markdown>=3.4,<4.0
